# Save in: logstash/pipeline/gdpr-pipeline.conf

input {
  file {
    path => [ "logstash/log/myapplication_gdpr.log" ]
  }
} 
output {
  elasticsearch {
      hosts => [ "elasticsearch:9200" ]
      index => "gdpr_logs"
  }
}